# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
*.session
*.session-*

# Media / model cache (anchored to repo root so they don't shadow source dirs
# like app/models — an unanchored "models/" matches at every depth in git).
data/media/
.cache/
/models/

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/

# OS
.DS_Store
